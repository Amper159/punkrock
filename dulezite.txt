# potřebný příkaz ke spuštění a aktualizaci koncertu
python import_punk_gigs.py
# načítání novinek
python punkradio/seed_news.py

#///💡 Postup:

Ulož tento skript jako deploy_to_pythonanywhere.sh do kořenové složky projektu.

Udělej ho spustitelným: chmod +x deploy_to_pythonanywhere.sh
Spust: ./deploy_to_pythonanywhere.sh
///#

🐍 PYTHONANYWHERE DEPLOY TAHÁK

1) Lokální změny (VS Code)
   git add .
   git commit -m "popis"
   git push

2) PythonAnywhere
   cd ~/punkrock-1/punkrock-1
   git pull

3) Reload webu
   Dashboard → Web → Reload
